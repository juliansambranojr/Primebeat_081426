---
id: 0349
date: 2026-09-06
type: formalization
title: WeilPowerGeom.lean: the geometric sum and the harmonic correction; the averaged range bounded by a range-free term plus a logarithm
refs: [lean_stage3/Stage3/WeilPowerGeom.lean::norm_one_sub_exp_ge, lean_stage3/Stage3/WeilPowerGeom.lean::norm_geom_le, lean_stage3/Stage3/WeilPowerGeom.lean::log_telescope, lean_stage3/Stage3/WeilPowerGeom.lean::norm_sum_exp_u_le]
supersedes: []
follows: 0348
sealed: false
---

**Question.** Block 13d of worksheet section `thirteen` 13 (`rung5.md#13d`): the geometric sum and the harmonic correction. Unit `unit_sigma` 0348 splits a member's normalized term at `m` into `exp(z(m+½)/π²)·exp(z·r m)` with `r m = u m − (m+½)/π²` inside `[0, 1/(π²(4m+6))]`. Summed over `m ∈ Ico a b`, the first factor is a geometric series in `ρ = exp(z/π²)` and the second is a correction. The question is whether the first sum can be bounded independently of the range length `b − a`, and the second by a logarithm of it, so that both are gains against the target's linear growth.

**What was proved.** `module_lines` 421 lines, `theorems_proved` 12 theorems, `defs` 2 definitions, `pins_module` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries. The geometric side runs on the separation of `ρ` from `1`. `norm_one_sub_exp_ge` reads that separation off the imaginary part alone: `‖1 − exp w‖ ≥ |Im(1 − exp w)| = exp(w.re)·|sin w.im|`, no hypothesis. `sin_im_ge` turns the phase into a linear quantity by Jordan's inequality, `Real.mul_le_sin` at `x = z.im/π² ≤ π/2`, which is the regime `z.im ≤ π³/2`; the two together give `rho_ne_one` and the explicit denominator `exp(z.re/π²)·2 z.im/π³`. `exp_shift` factors `exp(z(m+½)/π²) = exp(z/(2π²))·ρ^m` and `geom_Ico` sums `ρ^m` over `Ico a b`, so `norm_geom_le` closes: the sum's norm is at most `geomBound z a b`, the two endpoint values `exp(z.re(a+½)/π²) + exp(z.re(b+½)/π²)` over that denominator. Nothing in it grows with `b − a`; the phase alone pays for the whole range.

The correction side is a harmonic sum. `log_step` is `1/(m+1) ≤ log(m+1) − log m`, which is `Real.log_le_sub_one_of_pos` at `m/(m+1)`; `log_telescope` telescopes it over `Ico a b` by `Nat.le_induction`, and `harmonic_quarter` divides by the `4` in `4m+6 ≥ 4(m+1)` to give `Σ 1/(π²(4m+6)) ≤ (log b − log a)/(4π²)`. Per term, `norm_corr_le` writes `exp(z·u m) − exp(z(m+½)/π²) = exp(z(m+½)/π²)·(exp(z·r m) − 1)` and applies `Complex.norm_exp_sub_one_le`, whose hypothesis `‖z·r m‖ ≤ 1` is exactly the regime `‖z‖ ≤ π²(4a+6)` read at `a ≤ m`. `exp_lin_le_Mab` caps the geometric factor at an endpoint, the exponent being linear in `m`, so the sign of `z.re` picks the end. `norm_sum_exp_u_le` assembles them: `‖Σ_{Ico a b} exp(z·u m)‖ ≤ geomBound z a b + 2‖z‖·Mab z a b·(log b − log a)/(4π²)`. Against the target's `b − a` the first term is bounded and the second logarithmic, so the block's lost-factor line holds: none.

`u` enters as an abstract sequence with the bracket as a hypothesis, so the module imports nothing from unit `unit_sigma` 0348; `r_pos` and `r_le` there are that hypothesis at the shell, which is what the loop's parameterize-downstream rule asks. Two of the block's hypotheses are dropped where the proof does not use them, an unused binder being a warning (`TRAPS.md` row `row_unused` 20): `exp_lin_le_Mab` needs no separate `a ≤ b` beside `a ≤ m ≤ b`, and `norm_corr_le` needs no `1 ≤ a`.

**The build.** `errors_first` 5 error lines from `roots` 3 root causes. One was a `ring` after a `field_simp` that had already closed the goal (`TRAPS.md` row `row_ring` 1). One was `le_or_lt`, renamed `le_or_gt` on this Mathlib, which cost `pair_lines` 2 lines, the unknown identifier and the `rcases` that then had nothing to split; that class has no row and the retrospective adds one, row `row_new` 24. `cascade_lines` 2 lines were the pins' `sorryAx` cascade (row `row_cascade` 19). A `ring` that fell back and reported `Try this: ring_nf` was row `row_ringnf` 12 and cost no error. Second build clean with no warnings, `jobs_module` 8656 jobs; the package builds at `jobs_package` 8759 jobs. Built under the loop at version `loop_version` 11; the retrospective bumps it to `loop_next` 12 for the scratch pass this run moved ahead of the module, and adds the renamed-lemma row. Two scratch passes on the cast surface — `(z/(π:ℂ)^2).re`, `((u m : ℝ) : ℂ)`, `‖(r : ℝ) : ℂ‖` — ran before a line of the module was written, and every coercion in it compiled on the first build.

What the next slice needs. Block 13e of the worksheet, which reads `z`, `Mab` and the regime at the shell: `Im z = 4ε'Δ` and `|Re z| ≤ η` from the selection of unit `unit_phase` 0347's `ζ = ε' + iΔ`, `Mab ≤ e^{K'}` from the selection block's threshold, and the quartic phase error `q ≤ 1/(N e^{K'})` traced back to `h` and `λ`. That is the last piece of requirement (c) of worksheet section `thirteen` 13, read through the averaging route of rung `rung` 5, slice `slice` 14 of the switched-window ladder.
