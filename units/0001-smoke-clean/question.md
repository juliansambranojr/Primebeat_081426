# Question

Hand-written stand-in for the transcript bracket a real unit carries.

> Does `lab check` stay silent and exit clean on a unit whose every prose
> number has a line in its own `values.tsv`?

Posed 2026-09-02 while building Phase 0 of the lab design. This fixture is the
control for `units/0000-smoke`: same shape, same matching rule, every claim
covered.
