---
id: 0352
date: 2026-09-06
type: formalization
title: "WeilPowerShell.lean: one shell member's weighted sum by explicit constants, the first module built under the relay"
refs: [lean_stage3/Stage3/WeilPowerShell.lean::uSh_bracket, lean_stage3/Stage3/WeilPowerShell.lean::geomBound_le, lean_stage3/Stage3/WeilPowerShell.lean::shellSum_le, lean_stage3/Stage3/WeilPowerShell.lean::shellSum_le_abs]
supersedes: []
follows: 0351
context: block 13f of the rung 5 worksheet reads block 13e's geometric bound at one shell member and makes every constant explicit; it is the far-member bound any variant of the averaging route needs, and the first module built with the builder stopping at the first build and the foreman finishing the proofs
sealed: false
---

**Question.** Block `13f` of worksheet section `section` 13 (`rung5.md#13f`): one shell member's weighted sum, with every constant explicit. The member sits at `ζ = ε' + iΔ`, the target at `ε`, the window at `m + 1 = λh`. Unit `unit_phase` 0347's `re_S_sq_ge`, divided by the target's own principal amplitude, leaves the weighted term `coef · exp(z · uLam λ h)` with `coef = ζ²/ε²` and `z = 2(ζ² − ε²)`, since `h²σ(λh − 1) = uLam λ h` (unit `unit_ulam` 0350). The question is what block `13e`'s bound says about `|Σ_h Re(coef · exp(z · uLam λ h))|` once `α = 1/(λπ²)`, `β₀ = −1/(2λ²π²)` and `c = 1/(2λ³π²)` are read in, and what it costs. A second question rode on it: the run is the first under the relay of `LOOP.md` § 4b, the builder laying the module and stopping at its first build, the foreman closing what did not close on one try.

**What was proved.** `module_lines` 515 lines, `theorems_proved` 15 theorems, `defs` 4 definitions, `pins` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries. `zOf`, `coef`, `shellSum` are the objects above; `uSh` patches `uLam λ` at `k = 0` to the affine value, because block `13e`'s bracket hypothesis quantifies over every `k` and `uLam λ 0 = 0` breaks it for `λ ≥ 2`, while the sum runs over `h ≥ a ≥ 1` where the two agree termwise (`sum_uSh_eq`). `uSh_bracket` is that patched bracket at every `k`, unit `unit_ulam` 0350's `uLam_bracket` for `k ≥ 1`. `zOf_re`, `zOf_im`, `norm_zOf_le` and `norm_coef` read the constants: `z.re = 2(ε'² − Δ² − ε²)`, `z.im = 4ε'Δ`, `‖z‖ ≤ 2(ε'² + Δ² + ε²)`, `‖coef‖ = (ε'² + Δ²)/ε²`. `im_alpha_le` and `norm_zOf_c_le` are the regime lines of `13e` at the shell: with `ε', Δ ≤ 1/2` the phase per step `4ε'Δ/(λπ²)` is under `π/2` by `Real.pi_gt_three`, and `‖z‖·c` is under `1`.

The endpoint factor is where the selection threshold enters. `exp_end_le` bounds `exp(z.re(αh + β₀))` by `M = exp(2ηb/(λπ²))` for every `h` in `[a, b]`, in two cases: when `z.re ≥ 0` the threshold `ε'² − Δ² − ε² ≤ η` gives `z.re ≤ 2η` and the affine exponent is at most `αb`; when `z.re < 0` the exponent is negative once `2λh ≥ 1`, so the factor is under `1`. `MabGen_le` is the same at the two ends. `inv_exp_le` bounds `1/exp(z.re α)` by `exp(2(ε'²+Δ²+ε²)α)`, the price of a possibly negative `z.re` in the geometric denominator. `geomBound_le` then reads block `13e`'s `geomBoundGen` at the shell: at most `M·λπ³·exp(2(ε'²+Δ²+ε²)α)/(4ε'Δ)`. `shellSum_le` is the pinned statement: for `0 < ε', Δ, ε ≤ 1/2`, `ε'² − Δ² − ε² ≤ η`, `1 ≤ λ`, `1 ≤ a ≤ b`,

```
|shellSum| ≤ ((ε'²+Δ²)/ε²) · M · ( λπ³·exp(2(ε'²+Δ²+ε²)/(λπ²))/(4ε'Δ) + 2(ε'²+Δ²+ε²)(log b − log a)/(λ³π²) )
```

`shellSum_neg` says the sum is even in `Δ`, by conjugation termwise (`Complex.exp_conj`), and `shellSum_le_abs` restates the bound with `|Δ|` for a member below the target, `Δ ≠ 0`. Every number in the bound is a number in `ε, ε', Δ, η, λ, a, b`; at the assembly `η` is section `eight` 8's threshold and `M` is its `e^{K'}`.

Sizes, as the block priced them: with the three parameters at most `1/2`, `‖z‖ ≤ 3/2` and the exponential factor is under `1.17`, so the geometric part is about `9λM/(ε'Δ)` and the correction under `(3/2)·M·log(b/a)/(λ³π²)`, both times `(ε'²+Δ²)/ε²`. No factor is lost in `h` or in `b`. The `1/Δ` is the geometric price, and the shell below `Δ ≈ λ/(εX)` where this bound says nothing is unit `unit_decision` 0351's finding, handled there by the tight count and never here. No hypothesis outside the block's per-theorem lists was needed; the builder's three readings of the block's shapes are in `PINS.md`.

**The build, and the relay.** The builder ran `LOOP.md` § 0 to § 4 once, with four scratch passes on the cast surface, and stopped. `errors_first` 3 error lines from `roots` 1 root cause: an `nlinarith` on a product of three bounded factors, which multiplies hypothesis pairs and not triples, now `TRAPS.md` row `row_triple` 28; the other `pins_cascade` 2 lines are the pins reading `sorryAx`, row `row_cascade` 19. That step became the run's one `sorry`, `sorries_first` 1, with its goal in a comment. The foreman closed it with the factorwise chain `mul_le_mul` then `mul_le_mul_of_nonneg_right`, `foreman_edits` 1 edit and `foreman_builds` 1 build, clean with all four pins. The builder resumed in the same context for § 5 and the scaffold: module at `jobs_module` 8668 jobs, package at `jobs_package` 8761 jobs, `traps_added` 4 trap rows (`row_first` 25 to `row_triple` 28) and the names it verified. Built under the loop at version `loop_version` 14, the version that introduced the relay; the retrospective bumps it to `loop_next` 15 for one wording, since the second build after the sorry cannot be clean: the pins of a sorried theorem fail until its proof is closed.

The measure the run was for. The builder's first pass read `tokens_first` 164440 tokens over `calls_first` 32 calls in `minutes_first` 16 minutes; its second pass ended with the usage line at `tokens_end` 177604 tokens after `calls_second` 15 more calls in `minutes_second` 3 minutes. Run `13e` (unit `unit_ulam` 0350, `module_lines_13e` 430 lines, `theorems_13e` 9 theorems, no relay) read `tokens_13e` 138681 tokens over `calls_13e` 40 calls in `minutes_13e` 11 minutes. So this module cost the builder more in total for a module a third larger, and about the same per theorem. What moved to the foreman was one step, because every proof but that one closed on the builder's single try from the block's hints. The relay's saving is bounded by what the builder would otherwise have iterated on, and on this module that was one lemma. What the first pass paid for is the statements, the scratch passes and the first build, which the relay leaves where they were.

What remains. Block 13f closes requirement (c) of section `section` 13 as far as far members go, read through the averaging route of rung `rung` 5, slice `slice` 16 of the switched-window ladder. The next block is the near members' sign or the tight-count leaf of unit `unit_decision` 0351, and both wait at Julian's word.
