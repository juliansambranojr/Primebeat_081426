**Files created:**
- `lab/chain.py` — The chain module (285 lines). Core functions: `spreadsheet_label` (bijective base-26 labeling), `handoff_digest` (sha256 over sorted unit ids), `build_forest` (builds the tree from `follows:` relationships), `compute_segments` (groups units into bounded segments with deterministic labels), `render_chain`/`parse_chain` (CHAIN.tsv serialization), `_check_disagreements` (detects label/new/missing findings against on-disk state), `chain` (public API), `run` (CLI entry point).

- `tests/test_phase4.py` — 50 tests covering spreadsheet labels (A through AAA), handoff digest properties, forest construction, linear chains, fork detection, gap detection, branch naming (dotted labels including depth-2 C.A.A), multiple branches ordered by first unit id, label determinism, disconnected roots, CHAIN.tsv roundtrip and idempotency, disagreement detection (label/new/missing), chain computation on temp trees, the real tree, and the CLI subcommand.

- `CHAIN.tsv` — Generated segment file. 7 segments, 8 units in chain, 5 unchained fixtures, 1 gap (0308 follows 0307), 1 fork (0308 has followers 0309 and 0310). The fork gives 0310 the branch label F.A.

**Modified files:**
- `lab/cli.py` — Added `lab chain` subcommand with `--cwd` and `--segment-size` flags.
- `lab/__init__.py` — Docstring updated to mention Phase 4.
- `tests/test_phase2c.py` — 2 tests updated for 0310 existing.
- `analysis/2026-09-02/lab_design.md` — Phase 4 row filled in.

**Gate results:**
- `python3 -m pytest -q` — 312 tests pass (262 original + 50 new)
- `lab check` — 0000 fails (by design), 0001-0004 pass, 0308 baselined, 0309 passes
- `lab chain` — runs and reports the chain (exit 0, 7 segments, detects the fork and gap correctly)
- `python3 utilities/check_refs.py` — 0 broken references
