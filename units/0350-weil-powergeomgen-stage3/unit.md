---
id: 0350
date: 2026-09-06
type: formalization
title: "WeilPowerGeomGen.lean: the geometric bound with a free slope, and u at general lambda"
refs: [lean_stage3/Stage3/WeilPowerGeomGen.lean::norm_geom_le_gen, lean_stage3/Stage3/WeilPowerGeomGen.lean::norm_corr_le_gen, lean_stage3/Stage3/WeilPowerGeomGen.lean::norm_sum_exp_u_le_gen, lean_stage3/Stage3/WeilPowerGeomGen.lean::uLam_bracket]
supersedes: []
follows: 0349
context: block 13e of the rung 5 worksheet restates block 13d with the slope, the intercept and the correction scale free, and brackets the normalized tail sum at general lambda; it is the last piece before the shell reading of block 13f
sealed: false
---

**Question.** Block `13e` of worksheet section `thirteen` 13 (`rung5.md#13e`): the geometric bound with a free slope, and `u` at general `λ`. Blocks `13c`–`13d`, units `unit_sigma` 0348 and `unit_geom` 0349, were stated at `λ = 1`, where `m + 1 = h`, the slope is `1/π²`, the intercept `½/π²` and the correction scale `1/(4π²)`. The shell needs `λ` free: unit `unit_phase` 0347's quartic phase error is `q = ‖w‖⁴/(π⁴(m+1)³) = |ζ|⁴h/(π⁴λ³)` at `m + 1 = λh`, and the assembly needs `4q·Σ_i (|ζ_i|²/ε²)·e^{K'} < ½` at the top of the range, so `λ³ ≥ 4N e^{K'}|ζ|⁴H/(π⁴ε²)`: `λ` grows with the range and is polynomial in `N`, `e^{K'}` and `1/ε`. The question is whether the geometric bound and the harmonic correction survive a free slope, and what the exponent looks like at general `λ`.

**What was proved.** `module_lines` 430 lines, `theorems_proved` 9 theorems, `defs` 3 definitions, `pins_module` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries. The geometric side is block `13d` with the affine exponent `αh + β₀` in place of `(h+½)/π²`. `exp_shift_gen` factors `exp(z(αh+β₀)) = exp(zβ₀)·exp(zα)^h`; `sin_im_ge_gen` is Jordan's inequality at the phase `z.im·α`, `Real.mul_le_sin` on `[0, π/2]`, which is the regime `z.im·α ≤ π/2`; `rho_ne_one_gen` separates `ρ = exp(zα)` from `1` by `exp(z.re α)·2 z.im α/π`, reading unit `unit_geom` 0349's `norm_one_sub_exp_ge` at `w = zα`. `norm_geom_le_gen` closes: the sum over `Ico a b` has norm at most `geomBoundGen z α β₀ a b`, the two endpoint values over that separation. It does not grow with `b − a`, and it grows like `1/α` as the slope falls — at `α = 1/(λπ²)` it is block `13d`'s bound times `λ`, the phase advancing `λ` times slower per step. That factor is the price of the slower phase and is polynomial in the range; no factor is lost.

The correction side keeps unit `unit_geom` 0349's harmonic machinery whole. `norm_corr_le_gen` writes `exp(z·u h) − exp(z(αh+β₀)) = exp(z(αh+β₀))·(exp(z·r h) − 1)` with `r h = u h − (αh+β₀)` and applies `Complex.norm_exp_sub_one_le`, whose hypothesis `‖z·r h‖ ≤ 1` is the regime `‖z‖·c ≤ (a:ℝ)+1` read at `a ≤ h`, since `‖z‖·c/(h+1) ≤ (a+1)/(h+1) ≤ 1`. `exp_lin_le_MabGen` caps the geometric factor at an endpoint, the exponent being affine and increasing in `h`, so the sign of `z.re` picks the end. `norm_sum_exp_u_le_gen` assembles them against `WeilPowerGeom.log_telescope`, unchanged from unit `unit_geom` 0349: `‖Σ_{Ico a b} exp(z·u h)‖ ≤ geomBoundGen z α β₀ a b + 2‖z‖·c·MabGen z α β₀ a b·(log b − log a)`. Bounded plus logarithmic against the target's linear range, as before.

`c ≥ 0` is not a hypothesis. The bracket at `h = 0` reads `0 ≤ c/1`, and that is what makes the correction sum's coefficient `2‖z‖·c·MabGen` nonnegative, which `Finset.sum_le_sum` needs. `u` stays an abstract sequence with the bracket as its hypothesis, so the general statements do not mention `λ` at all; that is the loop's parameterize-downstream rule, and it is why the shell can instantiate them at whatever slope the fixed point picks.

The `λ` side is `uLam λ h = h²·σ(λh − 1)`. `uLam_eq` is the cast: with `1 ≤ λh`, `((λh − 1 : ℕ):ℝ) + 1 = λh`, so `uLam λ h = u(λh − 1)/λ²`. `uLam_bracket` divides unit `unit_sigma` 0348's `r_pos` and `r_le` at `m = λh − 1` by `λ²`: the linear part `((m:ℝ)+½)/π²` becomes `h/(λπ²) − 1/(2λ²π²)`, slope `1/(λπ²)` and intercept `−1/(2λ²π²)`, and the correction is between `0` and `(1/(2λ³π²))/((h:ℝ)+1)`, correction scale `1/(2λ³π²)`. The last step is `λ²π²(4λh+2) ≥ 2λ³π²(h+1)`, which reduces to `λh + 1 ≥ λ` and holds because `1 ≤ h`. The block's identity, checked in Lean rather than by hand.

One place the block's bookkeeping was off: it lists `sin_im_ge_gen` under `G2` and `G3` alone, and the proof also needs `G1`, since `0 ≤ z.im·α` is Jordan's left endpoint and `z.im > 0` does not give it without `α > 0`. `G1` is in the block's own regime list, so the statement takes it there and nothing outside the block is assumed.

**The build.** `errors_first` 1 error line from `roots` 1 root cause: a bare `ring` after a `field_simp` that had already closed the goal, `TRAPS.md` row `row_ring` 1, in the one `field_simp` of `uLam_eq` that the module's other two had the `try` on. No trap row was added; that class already has its row. Second build clean with no warnings, `jobs_module` 8667 jobs; the package builds at `jobs_package` 8760 jobs. Built under the loop at version `loop_version` 12; the retrospective bumps it to `loop_next` 13, adding to section `four` 4 the grep that lists every `ring` on the line after a `field_simp`, which is exactly the line this run paid for. One scratch pass on the cast surface — the real-cast `re` and `im`, the two exponential factorizations, the nat-subtraction cast at `λh − 1`, the norm of `z` times a real — ran before a line of the module was written, and every coercion and every name in it compiled on the first build.

What the next slice needs. Block `13f` of the worksheet, which reads `z = 2(ζ² − ε²)`, `α = 1/(λπ²)`, `c = 1/(2λ³π²)` and `u = uLam λ` at one shell member and bounds `|Σ_h Re((ζ²/ε²)·exp(z·uLam λ h))|` by explicit constants in `ε, ε', Δ, η, K', λ, a, b`. With that, requirement (c) of worksheet section `thirteen` 13 closes, read through the averaging route of rung `rung` 5, slice `slice` 15 of the switched-window ladder.
