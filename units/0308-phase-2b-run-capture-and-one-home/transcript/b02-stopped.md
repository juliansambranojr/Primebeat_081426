# Transcript block b02 — agent acafdbdc4f5818254, role build-stopped

Unit 0308. Spawned `general-purpose`, described `Phase 3: run capture and the
one-home rule`. Julian interrupted it. It wrote most of what the phase shipped
and it never reported, so this block is not a report — it is the record that
there was none, which the design's § The fingerprint asks for by name: "An
agent that was stopped keeps its entry, because what it produced before it
stopped and what a successor discarded is part of the record."

Its transcript is
`~/.claude/projects/<project>/<session>/subagents/agent-acafdbdc4f5818254.jsonl`,
outside the repository, unversioned, and gone on a machine change.

The last three things it said, and the interruption, verbatim from that file:

> Julian added spec sections mid-build. Pulling and reading before I go further.

> Read. Two sections change my build. Let me finish the `-e` fix, then check `check_direct_run.py`.

> Now building the two fixture units in the tree, so the demonstration lives in a unit rather than a scratchpad.

> [Request interrupted by user]

Its last action was a write of `units/0004-run-fails/run/run.sh`, which is in
the tree as it left it.

What the successor kept and what it discarded is stated in block b01 under its
own heading "The interrupted work", and this unit's prose does not restate it.
