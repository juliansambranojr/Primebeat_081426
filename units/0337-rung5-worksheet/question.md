> Source: session transcript, 2026-09-06 (this session), after unit 0336 was reported

> You said the analysis lives in your head and leaves no trace. Where is your “head “ exactly? I ask because what of we off load that to a file so it get free up attention to focus on other things

> Yes

CONTEXT.md: § Current state of the world (the Lean tree as of entry 170; this decision postdates it).

NOTEPAD: no line yet.

From `lean_stage3/LOOP.md`, step one as changed:

```
The derivation lives in a file, never only in the session. Each rung has
a worksheet, `lean_stage3/design/<rung>.md`, with one section per piece
of analysis, each marked PROVED (names the Lean theorem) or SKETCH (the
numbers and the plan). Do the analysis there first: the quantities and
their sizes, the regime conditions, the constants, the comparison, the
open questions. Append as you go. Re-read it instead of re-deriving. A
compaction loses nothing in it, and a fresh instance starts from it.
```

From `lean_stage3/design/rung5.md`, the section the assembly turns on:

```
    |term_other| / term_target ≲ poly(h, m) · exp( [ (ε'² − Δ²) − ε² ] · h/(π²λ) · c )
```
