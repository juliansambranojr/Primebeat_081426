---
id: 0354
date: 2026-09-07
type: formalization
title: "WeilPowerNear.lean: the near members' sign; the second module under the relay, the first at loop version 16"
refs: [lean_stage3/Stage3/WeilPowerNear.lean::bracket_nonneg, lean_stage3/Stage3/WeilPowerNear.lean::term_nonneg, lean_stage3/Stage3/WeilPowerNear.lean::shellSum_nonneg, lean_stage3/Stage3/WeilPowerNear.lean::shellSum_nonneg_abs]
supersedes: []
follows: 0353
context: block 13g of the rung 5 worksheet is the helping half of the member split, the members whose phase stays under a right angle over the whole range; with 13f's far bound it leaves only the shell of unit 0351 open, and it is the first block whose Theorems lines were Lean statements copied by the builder
sealed: false
---

**Question.** Block `13g` of worksheet section `section` 13 (`rung5.md#13g`): the near members' sign. A member at `ζ = ε' + iΔ` with `0 < Δ < ε'` contributes at each `h`, relative to the target at `ε`, the real part `e^{z.re·u}/ε² · ((ε'² − Δ²) cos θ − 2ε'Δ sin θ)` with `θ = 4ε'Δ·u` and `u = uLam λ h`, unit `unit_shell` 0352's objects and unit `unit_phase` 0347's bracket at the shell. Is it nonnegative at every `h` in the range once the phase at `b` is small, so that these members help the target? A second question rode on it: the block's Theorems lines were the Lean statements verbatim and the composed statements were quoted under Composes, so the builder opened no other module; the run measures that against unit `unit_shell` 0352, and it is the first run under loop version `loop_version` 16, where five of the loop's rules are checks.

**What was proved.** `module_lines` 236 lines, `theorems_proved` 9 theorems, `defs` 0 definitions, `pins` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries. `cos_ge_lin` is Jordan's inequality turned for the cosine: `1 − 2θ/π ≤ cos θ` on `[0, π/2]`, from `Real.mul_le_sin` at `π/2 − θ`. `bracket_nonneg` is the sign: with `0 < Δ < ε'`, `0 ≤ θ` and the crude condition `θ·(2ε'Δ + 2(ε'² − Δ²)/π) ≤ ε'² − Δ²`, the bracket `(ε'² − Δ²) cos θ − 2ε'Δ sin θ` is nonnegative, because `sin θ ≤ θ` and the cosine bound turn it into the condition itself, and `θ ≤ π/2` follows from the condition since its coefficient is at least `2(ε'² − Δ²)/π`. `re_term_eq` is the identity: the weighted term's real part equals `exp(2(ε'² − Δ² − ε²)u)/ε²` times that bracket at `θ = 4ε'Δu`, from `Complex.exp_re`, `exp_im` and the real and imaginary parts of `coef` and `zOf·u`. `uLam_nonneg` and `uLam_le` bracket the phase variable: `0 ≤ uLam λ h ≤ h/(λπ²)`, the upper half being unit `unit_ulam` 0350's `uLam_bracket` with the correction `1/(2λ³π²(h+1))` under the intercept `1/(2λ²π²)`, which is `λ(h+1) ≥ 1`. `phase_le` moves the phase at `h` up to its value at `b`. `term_nonneg` composes them: under the regime `0 < Δ < ε'`, `0 < ε`, `1 ≤ λ`, `1 ≤ h ≤ b` and the condition N2 at `b`, every term is nonnegative. `shellSum_nonneg` sums it over `Ico a b` with `1 ≤ a`, and `shellSum_nonneg_abs` restates it for a member below the target through unit `unit_shell` 0352's `shellSum_neg`.

Sizes, as the block priced them: for `Δ ≪ ε'` the condition N2 reads `4ε'Δb/(λπ²) ≲ π/2`, so `Δ ≲ λπ³/(8ε'b)`, which is unit `unit_decision` 0351's near boundary. Between it and 13f's far bound sits the shell that unit priced. No factor is lost; the block has no constant to lose, its output is a sign. No hypothesis outside the block's lists was needed and no shape reading was taken: the nine statements are the block's lines, copied.

**The build, and the relay.** The builder ran the loop at version `loop_version` 16 to its first build and stopped. `errors_first` 5 error lines from `roots` 1 root cause: `nlinarith` on the final combination of `bracket_nonneg`, the step the block's hint named, with the `pins_cascade` 4 pins reading `sorryAx` behind it, row `row_cascade` 19. That step became the run's one `sorry`, `sorries_first` 1, with its goal in a comment. The foreman's first try, `linarith` on the three hypotheses, failed the same way: `linarith` takes each product as one atom, so `hθ`'s product and `hcos`'s product never meet. One `ring` identity rewrites the two into the same atoms and `linarith` closes; `foreman_edits` 2 edits, `foreman_builds` 2 builds. The builder recorded it as `TRAPS.md` row `row_new` 29, `traps_added` 1 row, with the fix column corrected to the chain that worked. Three steps the block's hint stated as a consequence, clearing `π` from the condition to get `θ ≤ π/2`, the builder had to find itself, and did. The retrospective adds both to `DESIGN.md`'s checklist and moves the deletion of `Scratch.lean` to the builder's second pass in `LOOP.md`, version `loop_next` 17; the five checks added at version 16 passed on the first run of the checker.

The measure. The builder's first pass read `tokens_first` 104519 tokens over `calls_first` 22 calls in `minutes_first` 9 minutes; the second pass ended with the usage line at `tokens_end` 114479 tokens after `calls_second` 12 more calls in `minutes_second` 2 minutes. Unit `unit_shell` 0352, the relay with the builder reading the composed module and interpreting the Theorems lines: `tokens_13f` 177604 tokens at the end for `theorems_13f` 15 theorems. Unit `unit_ulam` 0350, no relay: `tokens_13e` 138681 tokens for `theorems_13e` 9 theorems. Per theorem, in thousands of tokens: `per_13e` 15.4, `per_13f` 11.8, `per_13g` 12.7. This module and 13e have the same theorem count, and the builder's cost fell by `saving_vs_13e` 17 percent; the loop it read fell from `words_old` 1847 to `words_new` 840 words and it opened no second module. What it did not fall to is the size of the proving alone, since the first pass is still the statements, the scratch pass and the first build.

What remains. With 13f and 13g the members split into helping, bounded, and the shell between, which is unit `unit_decision` 0351's finding and needs the tight count. The next block is the assembly's statement with that leaf named, or the count itself; both wait at Julian's word. Rung `rung` 5, slice `slice` 17 of the switched-window ladder.
