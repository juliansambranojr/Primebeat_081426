---
id: 0002
date: 2026-09-02
type: run
title: Sealed smoke fixture that lab check must verify
refs: [0001]
supersedes: []
sealed: true
---

**Permanent test fixture.** This unit exists so that `lab check` has a SEALED
unit to verify, and it is committed on purpose. Do not edit it and do not
delete it — editing any file in it is exactly what the check is built to
catch, so an edit here fails the suite. Its unsealed twins are the two smoke
fixtures beside it, one of which is unit 0001. The tamper test copies this
unit into a temporary directory and damages the copy.

**What it claims.** The ladder ratio came out at 3.07, the residual at 0.0184,
and the census counted 422 rows. Every one of those has a line in
`values.tsv`, generated by `lab values` from the results JSON in `run/`.

**What the digest excludes.** The run also recorded an elapsed time and the
moment it was generated. `lab values` filed both under `meta.`, and the unit
digest is computed with those lines removed, so a re-run that reproduces the
measurements matches on the digest even when it took a different number of
seconds. The elapsed time is still evidence: a prose claim of 12.5 seconds
would have a line to point at.

**Exempt by pattern.** The date 2026-09-02 is a date. This unit's own id is
exempt, and so is unit 0001, which it names in `refs`.
