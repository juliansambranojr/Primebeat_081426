---
id: 0343
date: 2026-09-06
type: run
title: Probe: the module loop on a Sonnet agent, rung 5 section 8, after the Opus agent's fixes
refs: [none]
supersedes: []
follows: 0342
sealed: false
---

**Question.** Two questions, one run. Does the module loop at version `loop_ver` 7 carry a weaker agent than the last one from a SKETCH section to a committed module, with nobody answering questions? And did the fixes the Opus agent's run produced (unit `0340`, unit `0342`) remove the defects it named, so that a new agent meets none of them? The section is `eight` (`rung5.md#8`), selection of the target: a maximum over a finite set of zeros and an induction with a bounded-increase step, a different shape from section `seven`'s inequalities. EXPLORATORY: no prereg in `preregs/`, the predictions below stand in for one and were written before the spawn.

**What ran.** One subagent, spawned from this session at `15:25` through the Agent tool with the model option `sonnet`; the harness resolves the version; the prompt does not ask for it and the report did not name it. The prompt is `run/prompt.md`: the Opus agent's prompt with the section changed, `prompt_lines_changed` 2 lines differing in `diff`. The agent runs in the working tree on `main`; its commits land through the pre-commit. The orchestrator writes nothing into the agent's module or unit. The report is copied to `run/report.md` as returned.

**What it shows.** Predictions, before the run. Baseline for this agent tier is the Opus run: `errors_first` `opus_errors` 5, `opus_minutes` 14 minutes to the first commit, `opus_defects` 7 defects named. The measures, read from the tree after the agent stops: `commit`, a commit on `main` with a new module, its import, and a unit passing `check_lean_unit.py`, predicted 1. `errors_first`, from the agent's unit, predicted between `pred_errors_lo` 8 and `pred_errors_hi` 20. `minutes`, spawn to first commit from `git log`, predicted between `pred_min_lo` 20 and `pred_min_hi` 50. `restarts`, predicted `pred_restarts` 0. `pins`, predicted 1. `worksheet`, section 8 flipped to PROVED in the same commit as the module, predicted 1 now that step 1 says so. `recurrence`, how many of the Opus agent's seven named defects this agent hits again, predicted `pred_recurrence` 0; each recurrence means the fix did not take. `new_defects`, places this agent names that the Opus agent did not, predicted at least 1.

Decision rule, the same as unit 0340's, quoted in `question.md`: transfers if `commit` is 1 with no restart and `errors_first` at most `pred_errors_hi` 20 (the band widened for the tier); weakly with a restart; fails without a commit, and then the stopping place is the loop's next edit. The fixes held if `recurrence` is 0.

**Outcome.** Transferred, and the fixes held. The agent's report is `run/report.md`, verbatim. Read from the tree: `commit` 1, the module `WeilPowerSelect.lean` at `0c97dec` with `module_lines` 190 lines, `theorems_proved` 7 theorems, `defs` 1 def, `sorries` 0 sorries, `pins_module` 4 axiom pins, imported in `Stage3.lean`, the package building, and unit `0344` passing `check_lean_unit.py`; `errors_first` 3, below the predicted band and below the Opus agent's; `minutes` 19 from the spawn to the commit, inside the band; `restarts` 0; `pins` 1; `worksheet` 1, section 8 marked PROVED in the module's commit as version 7 of the loop says; `recurrence` 0, none of the Opus agent's seven defects met again; `new_defects` 3 named, one now `TRAPS.md` row B9 (a `noncomputable def` and a pin placed after the namespace's `end` are invisible to the checker's greps), one a loop gap (the unit's `run/run.sh` is hand-written and the loop never said what goes in it; the agent copied unit `0341`'s), one in the worksheet (section 8's Lean-shape line named two ingredients as one module). The agent made no edit to `LOOP.md`, so the run.sh gap is fixed here: version `loop_ver_after` 8 states the file's content. Checker runs: `lab check` `lab_runs` 3 runs, none refused; `check_lean_unit.py` `checker_runs` 2 runs, the first refused on DEFS and PIN; `refusals` 0 pre-commit refusals. Cost from the harness's usage line: `tokens` 199021 tokens over `tool_uses` 86 tool calls. The agent did not name its version. Files touched by its commit: `files_touched` 11, all inside the module, `Stage3.lean`, the worksheet section, `TRAPS.md`, `PINS.md` and the unit.

The mathematics came back with two things the worksheet did not have. The pigeonhole lemma (`exists_step_le`) needs no monotonicity of the real-part sequence, only that its square is bounded at every step; monotonicity is used once, for the closing fact that the target's real part is at least the box's (`target_ge_box`). And the target's height is `T + h/(8K'π²λ)` with no `+1`, since the step count is a floor plus one and the sketch's slack came from rounding. The module takes the largest real-part offset per box as a parameter `E : ℕ → ℝ`; wiring it to a finite zero set is left to the module that needs it, the same choice the loop's step 3 prescribes. The orchestrator wrote nothing into the module or unit 0344.

What the two probes together show: two agents of different tiers, each seeing the repo for the first time, took the two SKETCH sections to committed, gate-checked modules in 14 and 19 minutes with 5 and 3 first-build errors and no message from anyone. The defects the first named did not recur for the second. The second named three the first had not, and each is now a row or a line. The loop is the thing that carries the work; the agent tier moved the error count and the minutes by less than the section did.
