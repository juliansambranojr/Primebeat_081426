> Source: session transcript, 2026-09-07, after unit 0354

> I was thinking of the dyadic table as arithmetic and rate of change… how the researchers called the only bump in a smooth curve. The bump is the deep zero and what they say you can’t see cause it multiplies and is covered over because they are lokk ok in with analysis but my posit is that the arithmetic is still there doing its thing

> Same dynamics cause the arithmetic and our work is primary

> Let’s test the posit, I’m down

> Lock

From `preregs/induction_bump_difference_table_v1_20260907.md`, the rule:

```
- `structure_beyond_overlay` — `excess ≥ 2`.
- `overlay_only` — `excess ≤ 0`.
- `ambiguous` — `excess = 1`.
```
