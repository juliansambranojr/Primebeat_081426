---
id: 0357
date: 2026-09-07
type: formalization
title: "WeilPowerClean.lean: the clean shell, a sub-range no member blocks; the first module a builder closed with no errors"
refs: [lean_stage3/Stage3/WeilPowerClean.lean::blocked_card_le, lean_stage3/Stage3/WeilPowerClean.lean::exists_unblocked, lean_stage3/Stage3/WeilPowerClean.lean::exists_clean_shell, lean_stage3/Stage3/WeilPowerClean.lean::exists_clean_shell_of_count]
supersedes: []
follows: 0356
context: unit 0351 killed the averaging route on the intermediate shell and named a zero-count leaf as what would close it; this block is the combinatorial half of that closure, the step that turns a count into a sub-range where every member is settled, and it corrects the constant 0351 attached to the leaf
sealed: false
---

**Question.** Block `13h` of worksheet section `section` 13 (`rung5.md#13h`). Unit `unit_decision` 0351 found the averaging route dead at the members whose phase neither stays small over the range (block `13g`, unit `unit_near` 0354) nor turns fast enough for the geometric bound to beat the range length (block `13f`, unit `unit_shell` 0352). It named a short-interval zero count as the leaf that would close it. The combinatorial content of that closure is the question here: cut the range into shells at a fixed ratio, and ask how many shells one member can block and how many shells a bound on the member count leaves clean.

**What was proved.** `module_lines` 151 lines, `theorems_proved` 4 theorems, `defs` 0 definitions, `pins` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries, importing `Stage3.Statement` alone. `blocked_card_le` is the window: if every shell in a finite set `B` has `lo ≤ Δ·aρ^k ≤ hi`, and `hi < lo·ρ^(W+1)`, then `B` has at most `W + 1` elements. The proof is the two ends of `B`: at its least element the lower bound holds, at its greatest the upper does, and the ratio between the two scales is `ρ` to the gap, so the gap cannot reach `W + 1` without the window being wider than it is. `exists_unblocked` is the pigeonhole: a finite index set `S`, a blocking set per index of size at most `W`, and more than `S.card·W` shells leave one shell in no blocking set. `exists_clean_shell` composes them and `exists_clean_shell_of_count` takes the count as a bound `N` on `S.card`, which is the shape the leaf will supply.

Nothing in the module mentions a zero, a window function or `λ`. The shells are `a·ρ^k` written out, the member's parameter is a bare real, and the blocking sets are given rather than computed. That is the loop's parameterize-downstream rule at its cleanest: the module states no definition at all, and block `13i` will discharge the `hB` hypothesis from `13f` and `13g` instead of assuming it.

**What the block corrected.** Unit `unit_decision` 0351 priced the leaf at `c1_old` 1.23, which is `1/(2 ln 1.5)`: one blocked shell per member at a shell ratio of `1.5`. The window is wider than one shell. A member blocks every shell whose scale puts `Δ·X_k` inside `[lo, hi]`, so the count is `W + 1` with `hi < lo·ρ^(W+1)`, and the range the route needs is `T'` raised to `c₁(W+1) log ρ`. Letting the ratio fall with `W` following it, `(W+1) log ρ` tends to `log(hi/lo)`, so the requirement is `c₁·log(hi/lo) < 1`. The window's ratio is `8Cε'²/(π³ε²)`, carrying no `λ`, no `h` and no `T`, an absolute constant once the target is selected with `ε' ≈ ε`. At a ratio of `ratio_ex` 8 the requirement is `c1_new` 0.481. Trudgian's `trudgian` 0.112 still clears it and the tree's crude chain at about `crude` 7 still does not, so the leaf the route hangs on is the same statement with a tighter constant.

**The build, and the relay.** `errors_first` 0 errors from `roots` 0 root causes, `sorries_first` 0 sorries at the builder's stop, so `foreman_edits` 0 edits: the first module in this ladder that the builder closed whole, and the first run where the relay's second half had nothing to hand back. `warnings_first` 1 warning, the `push_neg` deprecation, now `TRAPS.md` row `row_new` 30, `traps_added` 1 row added. Module at `jobs_module` 8656 jobs, package at `jobs_package` 8763. Built under the loop at version `loop_version` 17.

Four things in the block were wrong and the builder named each. Its Module line pinned a loop version, which fixes a moving number in a block written before the run; it now says to use whatever version the run starts under. Its hint for `blocked_card_le` warned off `nlinarith` and prescribed a two-step chain where one `mul_le_mul` on the two factor bounds closes it. Its Composes list named two lemmas the proof never used and omitted `Finset.eq_empty_or_nonempty`, which the empty case needs. All four are corrected in the worksheet in this commit.

The measure. The builder's first pass read `tokens_first` 78312 tokens over `calls_first` 21 calls in `minutes_first` 5 minutes; the second ended at `tokens_end` 87464 tokens after `calls_second` 12 more calls in `minutes_second` 2 minutes. Against the two earlier relay runs, in thousands of tokens per theorem: `per_13f` 11.8 at unit `unit_shell` 0352, `per_13g` 12.7 at unit `unit_near` 0354, `per_13h` 21.9 here. This module is small enough that the fixed cost of the loop, the traps file and the scratch pass dominates, so the per-theorem figure rises while the run itself is the cheapest of the three. The comparison that holds across all three is the first-build error count: `errors_13f` 3, `errors_13g` 5, and 0 here, on a block whose Theorems lines were Lean statements the builder copied and whose Composes statements were quoted in full.

What remains. Block `13i` names the leaf, derives a short-interval count from `ArgCrude.StmtSCrude`, and discharges this module's `hB` from `13f` and `13g` so that the clean shell is produced rather than posited. Then the assembly of section `ten` 10. Rung `rung` 5, slice `slice` 18 of the switched-window ladder.
