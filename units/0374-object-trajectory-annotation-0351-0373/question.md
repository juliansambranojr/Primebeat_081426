> Source: session transcript, 2026-09-18, after prose_parser PLAN A to F and lab PLAN N, O, P closed

> ok, the 23 way tie is converging on a size but the trajectory is different to the size. so its not the same size

> if thats cheap what the expensive version

> so the expensive is only expensive in the construction of it but after it becomes cheap?

> ok, create the sub plan for the expensive cheap test and what happens if it passes or fails

> go

From lab `PLAN.md` § R.2, committed at 266a4da before this unit's directory existed:

```
Plateau: over the last 10 units in id order (0364 to 0373), `moves_after` rises by at most 1 and `kinds_after` rises by at most 1.
```
