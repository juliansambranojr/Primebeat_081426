---
id: 0363
date: 2026-09-08
type: formalization
title: "ArgLegs.lean: the two contour legs of zetaArgContour — pi per zero of Re zeta on the segment plus one, and pi/2 on the line Re s = 2"
refs: [lean_stage3/Stage3/ArgLegs.lean::im_integral_le_pi_of_re_nonneg, lean_stage3/Stage3/ArgLegs.lean::segment_im_integral_le, lean_stage3/Stage3/ArgLegs.lean::horizontal_leg_le, lean_stage3/Stage3/ArgLegs.lean::zetaArgContour_le]
supersedes: []
follows: 0362
context: the count-to-argument step is the leaf both trees wait on (NOTEPAD 2026-09-08); of its three blocks this is the analytic one, Backlund's argument along the two legs of the contour that unit 0274 identified argS with, built first because it was the likeliest to fail
sealed: false
---

**Question.** Block `H1` of the hNT worksheet (`hnt.md#h1`), the first of three toward `StmtSCrude argS`. At a good height `argS T = zetaArgContour T` (unit `unit_identity` 0274), and the contour is two legs: up the line `Re s = 2` from `2` to `2 + iT`, then left along height `T` from `2` to `1/2`. Backlund's bound is that the argument of `ζ` moves by less than `π` between consecutive zeros of `Re ζ` on the horizontal leg and stays within a right angle on the vertical one. Does that go through in Lean, on the tree's own `zetaArgContour`, with the count of those zeros left as a parameter?

**What was proved.** `module_lines` 393 lines, `theorems_proved` 10 theorems, `defs` 0 definitions, `pins` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries, and `zeta_refs` 61 references to `riemannZeta`.

The abstract half is for any path `g : ℝ → ℂ` with derivative `g'` and no zero on `[a, b]`. Where `0 ≤ Re g` the values lie in the slit plane, `log ∘ g` has derivative `g'/g` (`HasDerivAt.clog_real`), so `∫ g'/g = log g(b) − log g(a)` (`integral_logDeriv_eq_log_sub`) and its imaginary part is a difference of two arguments each within a right angle, at most `π` (`im_integral_le_pi_of_re_nonneg`); where `Re g ≤ 0` the same at `−g`. `re_sign_const` says `Re g` cannot change strict sign on `[a, b]` without a zero strictly inside, by the intermediate value theorem. `segment_im_integral_le` inducts on a finite set `Z` containing every zero of `Re g` inside `(a, b)`, splitting at the largest one, and gets `π(|Z| + 1)`.

The half at `ζ`: `vertical_leg_le` reads the leg `t ↦ ζ(2 + it)` with `Re ζ > 0` there as a hypothesis, so the real part of the integral of `logDeriv ζ` is `arg ζ(2+iT) − arg ζ(2)`, and `ζ(2) = π²/6` is real positive, giving `π/2`. `horizontal_leg_le` reads `x ↦ ζ(x + iT)` on `[1/2, 2]` at a good height with `Z` any finite set holding the zeros of `Re ζ` inside, giving `π(|Z| + 1)`. `zetaArgContour_le` puts them together: `|Stage3.zetaArgContour T| ≤ |Z| + 3/2`. That is the exact shape of Backlund's inequality with the count still a parameter, which block `H2` supplies.

**The build, and the relay.** `errors_first` 4 error lines from `roots` 2 root causes at the first build, `sorries_first` 2 sorries at the builder's stop, both the same trap: `ContinuousAt.comp` unified its implicit function with `HAdd.hAdd 2` at the wrong point, `TRAPS.md` row `row_comp` 16, at the continuity of `logDeriv ζ` along each leg. The foreman closed both by naming the two functions explicitly, `foreman_edits` 4 edits and `foreman_builds` 3 builds in all: the two closures, the removal of an unused `a ≤ b` from `re_sign_const` and its two call sites (row `row_binder` 20; the block is corrected to match), and the header sentence and the two goal comments, which carried the word the unit checker greps for. Eight of ten theorems closed on the builder's one try, including the segment induction, which the pin had named as the likeliest to need the foreman. `warnings_first` 1 warning at the first build, the unused binder. `traps_added` 1 row, `row_new` 32: a namespace read off the file's name rather than its `namespace` line, which the scratch pass caught before the build. `assumed_pins` 3 pins, all promoted into the block: the namespace is `Stage3`, the sign-constancy lemma takes the direct route with no closure argument, and the induction generalises both endpoints. Module at `jobs_module` 8718 jobs, package at `jobs_package` 8766. Built under the loop at version `loop_version` 18. One gate corrected on this unit: `check_lean_unit.py`'s HEADER check compared the design slug, lower-cased by construction, against the header's `H1` case-sensitively and refused; it now compares case-blind.

The measure. The builder's first pass read `tokens_first` 149052 tokens over `calls_first` 35 calls in `minutes_first` 15 minutes; the second ended at `tokens_end` 160159 tokens after `calls_second` 12 more calls in `minutes_second` 2 minutes. The largest block of the relay so far and the first with two sorries, both of one class.

What remains. Block `H2`: `Re ζ(2+it) > 0` from the Dirichlet series, and the finite set of zeros of `Re ζ` on the segment with its Jensen bound `15 log T + 73`, through `G_T(z) = ζ(2z+2+iT) + ζ(2z+2−iT)` on the template of the local zero count. Then `H3`, the good heights by the identity, the bad heights by the left limit, and the band at `band_a` 112, over entry 130's census budget of `budget` 100, as the worksheet head recorded before any module.
