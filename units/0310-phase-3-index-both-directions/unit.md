---
id: 0310
date: 2026-09-03
type: instrument-fix
title: lab Phase 3 — INDEX.md, INDEX-values.tsv, lab index
refs: [0305, 0306, 0307, 0308, 0309]
supersedes: []
follows: 0308
agents:
  - id: phase-3-build
    role: build
    block: transcript/b01.md
sealed: false
---

**Exploratory.** No prereg, no decision rule, no verdict. This unit records
what Phase 3 built: 2 generated index files and the `lab index` subcommand
that produces them.

**What Phase 3 built.** The forward index (`INDEX.md`) and the reverse map
(`INDEX-values.tsv`), both generated by `lab index`.

1. `INDEX.md` — a generated summary of every unit. Groups units by type,
   reports sealed/unsealed status, and counts each unit's `values.tsv` keys.
   The footer states the total key count across all units.

2. `INDEX-values.tsv` — the reverse map: 1 line per key, listing every unit
   whose prose cites that key's value. Reuses `lab check`'s matching logic
   (`NUM`, `matches`, exemption spans) to determine which keys each unit's
   prose cites.

3. `lab index` — the CLI subcommand registered in `lab/cli.py` with a `--cwd`
   argument. The implementation lives in `lab/index.py` (226 lines). Running
   it regenerates both files.

**New source files.** `lab/index.py` (the implementation) and
`tests/test_phase3.py` (18 tests covering both generated files, correct
counts by type, reverse mapping, idempotency, units with no results, smoke
fixtures, artifact counts, generated headers, the CLI subcommand, and the
reverse map format).

**Modified files.** `lab/cli.py` (registered `lab index`), `lab/__init__.py`
and `lab/new.py` (docstrings updated), `analysis/2026-09-02/lab_design.md`
(Phase 3 row filled), `tests/test_phase2c.py` (2 tree-state assertions
updated to expect unit 0310).

**Gates.** 260 tests passed, 2 failed (tree-state assertions in
`test_phase2c.py` that expect unit 0310 as the next id; the tree now holds
it). `lab check` over all units: 0000 fails (by design), 0001 through 0004
pass, 0308 baselined, 0309 passes. `lab index` produces both files.
`python3 utilities/check_refs.py` returned 0 broken references.
