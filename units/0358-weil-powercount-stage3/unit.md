---
id: 0358
date: 2026-09-07
type: formalization
title: "WeilPowerCount.lean: the short-interval count as a named leaf, and the range that closes unit 0351's circularity"
refs: [lean_stage3/Stage3/WeilPowerCount.lean::card_le_of_leaf, lean_stage3/Stage3/WeilPowerCount.lean::exists_range_log, lean_stage3/Stage3/WeilPowerCount.lean::count_lt_shells, lean_stage3/Stage3/WeilPowerCount.lean::exists_clean_shell_of_leaf]
supersedes: []
follows: 0357
context: unit 0351 killed the averaging route on a circularity between the range and the height it must sit inside; unit 0357 supplied the combinatorics that turns a zero count into a clean sub-range; this block names the count as a leaf with its budget and route and proves the circularity closed
sealed: false
---

**Question.** Block `13i` of worksheet section `section` 13 (`rung5.md#13i`). Unit `unit_decision` 0351 found that the range `[a, b]` must hold more shells than there are members, that the member count grows with the target's height, and that the height grows with the range through section `eight` 8's walk. At the tree's own count the range had to exceed the height raised to a power above `expo_old` 6, so nothing closed. Unit `unit_clean` 0357 showed a member blocks at most a bounded number of shells. What remains is the count itself: name it as a leaf with a budget and a discharge route, and decide whether the two demands on the range ever meet.

**What was proved.** `module_lines` 152 lines, `theorems_proved` 4 theorems, `defs` 1 definition, `pins` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries, importing `Stage3.WeilPowerClean`.

`StmtShortCount cnt c₁ c₂` is the leaf: the count at height `T` is at most `c₁ log T + c₂` for every `T` at least 2. Its budget is `c₁ ≤ budget` 0.48, which is unit `unit_clean` 0357's closure condition at a window ratio of `ratio` 8. Its route is Backlund's decomposition of the zero count over a unit interval into the phase increment over `π` and the change in the argument, the increment about half the log of the height from Stirling and the argument bounded by the statement `ArgCrude.StmtSCrude` the tree already carries: that gives `c₁ = 1/(2π) + 2B₁`, about `floor` 0.159 plus twice the argument's constant. Trudgian 2014 has `trudgian` 0.112, so `c₁` is about `c1_lit` 0.383 and sits inside the budget. The tree's own crude chain has about `crude` 7 and does not. The floor is there at `B₁ = 0`, so the budget is not vacuous and no argument bound alone can make it so.

`exists_range_log` is the closure. Write `L` for the log of the range's top, `D` for the log of the height's fixed part, `lρ` for the log of the shell ratio, `E` for the constant shell overhead, and `α = c₁(W+1)lρ`. The shells fit when `(c₁ M + c₂)(W+1)lρ + E ≤ L` for every `M` at most `D + L`, which is what the height's dependence on the range makes of the count. The theorem says that when `α < 1` such an `L` exists, and its witness is explicit: the constant part over `1 − α`. Since `L` is then linear in `D`, the range is the height raised to `α/(1 − α)` times a constant, so at `α = 1/2` the range is the height squared and at `α = 1/4` its cube root. That is the circularity gone: unit `unit_decision` 0351's exponent was above `expo_old` 6 and could not be brought under 1 by any choice inside the route, and here it is under 1 exactly when the leaf's constant is inside its budget.

`count_lt_shells` carries the real inequality down to the natural-number statement unit `unit_clean` 0357's pigeonhole wants, and `exists_clean_shell_of_leaf` composes the four: the leaf bounds the member set, the bound feeds the shell count, and the clean shell comes out. The module stays abstract in the count, the blocking sets and the member parameter, so nothing here decides what a member is; the assembly discharges that.

**The build, and the relay.** `errors_first` 0 errors from `roots` 0 root causes, `sorries_first` 0 sorries at the builder's stop, `foreman_edits` 0 edits, `traps_added` 0 trap rows: the second run in a row the builder closed whole. Module at `jobs_module` 8657 jobs, package at `jobs_package` 8764. Built under the loop at version `loop_version` 17. `warnings_first` 0 for this module; the one warning in its build log is upstream, the deprecation unit `unit_clean` 0357 rowed.

`assumed_pins` 2 pins, both the builder's and both right to record. The block gave the leaf's signature and not its body, so the builder read the body off a proof hint; a block that names a leaf should state the Prop, and this one now does. And the block's `0 ≤ M` hypothesis is unused by the proof; it is kept because the assembly reads `M` as the log of a height and will have it. The block is corrected on both, and on three hint details the builder found: `positivity` cannot read the sign hypotheses out of the context where `mul_nonneg` can, `field_simp` closes the cancellation with no `ring` after it, and the named-product route to `linarith` is the one that works rather than `nlinarith`.

The measure. The builder's first pass read `tokens_first` 78191 tokens over `calls_first` 23 calls in `minutes_first` 6 minutes; the second ended at `tokens_end` 84817 tokens after `calls_second` 10 more calls in `minutes_second` 1 minute. First-build errors across the five relay modules, in order: `errors_13f` 3, `errors_13g` 5, `errors_13h` 0, and 0 here. The two blocks that carried Lean statements verbatim with every composed statement quoted in full are the two that cost nothing to fix.

What remains. The leaf is open and is the only open thing on this route: a short-interval count with leading constant under `budget` 0.48. Its route runs through Stirling for the phase increment, which the tree has only at a crude band, so discharging it is sharp-constant work and is named as such. Everything else is the assembly of section `ten` 10: instantiate the blocking sets from blocks `13f` and `13g` so that the clean shell is produced rather than posited, then the weighted sum over the clean shell, then `StmtDetect`. Rung `rung` 5, slice `slice` 19 of the switched-window ladder.
