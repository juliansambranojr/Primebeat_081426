---
id: 0365
date: 2026-09-08
type: formalization
title: "ArgCount.lean: StmtSCrude argS 15 600, and 15 75 under no zero at height 2; the crude hNT bands (112, 0, 698) and (112, 0, 173)"
refs: [lean_stage3/Stage3/ArgCount.lean::argS_le_of_good, lean_stage3/Stage3/ArgCount.lean::argS_le_gt_two, lean_stage3/Stage3/ArgCount.lean::sCrude_holds, lean_stage3/Stage3/ArgCount.lean::sCrude_holds_of_good_two, lean_stage3/Stage3/ArgCount.lean::rvM_crude, lean_stage3/Stage3/ArgCount.lean::rvM_crude_of_good_two]
supersedes: []
follows: 0364
context: the third and last block of the count-to-argument step, the leaf both trees waited on (NOTEPAD 2026-09-08); H1 (unit 0363) bounded the contour, H2 (unit 0364) counted the zeros on it, and this block assembles StmtSCrude argS and reads the band off the tree's two composition theorems
sealed: false
---

**Question.** Block `H3` of the hNT worksheet (`hnt.md#h3`). With H1's leg bound and H2's `Finset`, does `StmtSCrude argS` close at every height `T ≥ 2`, the bad heights included, and what band does the tree's `RvM_of_phase_arg` then hand back?

**What was proved.** `module_lines` 364 lines, `theorems_proved` 15 theorems, `defs` 1 definition, `pins` 6 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries, and `zeta_refs` 32 references to `riemannZeta`.

Good heights. `good T` says no zero of `ζ` with positive real part sits on `Im s = T`. There `argS_eq_zetaArgContour` (unit 0274) rewrites `argS T`, H1's `zetaArgContour_le` takes `Z = reZeros hT` with H2's positivity and membership, and `card_reZeros_le` gives `|argS T| ≤ 15 log T + 73 + 3/2` (`argS_le_of_good`).

Bad heights above `2`. Upstream's zeros of positive height below `T₀` are finitely many, and `N` is their finite sum of orders (`N_eq_sum`, upstream's own proof copied), each order at least one, so `N` is nonnegative and monotone (`N_nonneg`, `N_mono`). `exists_good_below` takes the largest of those imaginary parts and of `a`, and any `T` strictly between it and `T₀` is good with no zero of imaginary part in `[T, T₀)`, so `N T = N T₀` (`N_eq_of_no_zero_between`, the two index sets equal). `phaseTheta` is half an integral of the continuous `phasePoint` less a `log π` term, so on `[T₀ − 1, T₀]` it is Lipschitz with a constant from `phasePoint_bound_near` (`phaseTheta_sub_le`). With `δ` at most `π` over that constant, the phase moves `argS` by at most a half, and `argS_le_gt_two` lands `15 log T + 75` for every `T > 2`.

The point `T = 2`. A bad `2` has no good height below it inside the tree's `2 ≤ T`, and the tree has no first-zero height. So `argS_two_le` compares upward with a good `T'` in `(2, 3)`: `0 ≤ N 2 ≤ N T'`, and `N T'` is `argS T'` plus the phase term, which `backlundPhase_holds` keeps within `97 log T' + 98` of the main term, itself at most `mainterm_abs` 3 on `[2, 3]` (`mainTerm_abs_le`). With `log T' ≤ 2` and `log 2 ≤ 1` the point costs `sc_b3_two` 600. So `sCrude_holds` is `StmtSCrude argS 15 600`, and `sCrude_holds_of_good_two` is `StmtSCrude argS 15 75` under `good 2`. The tree's `backlundArg_of_identity` and `RvM_of_phase_arg` then give `Riemann_vonMangoldt_bound` at `band_a` 112, `band_b2` 0 and `band_b3` 698 unconditionally (`rvM_crude`), and at `band_b3_good` 173 under `good 2` (`rvM_crude_of_good_two`).

**The band against the budget.** Entry 130's census budget for the first coefficient is `budget` 100, and the band's `band_a` 112 is over it by `over` 12. That was recorded in the worksheet head before H1's module, and it is the Stirling half's `stirling_a` 97 that would have to come down; the Jensen `count_a1` 15 is already crude-tight. The additive constant `band_b3` 698 is one point's cost: `good 2` is a single fact about `ζ` the tree does not hold, and with it the band is `(112, 0, 173)`.

**The build, and the relay.** `errors_first` 7 error lines from `roots` 2 root causes at the first build, `sorries_first` 1 sorry at the builder's stop. The first root was a name: the block's `IsCompact.exists_bound_of_continuousOn'` is the multiplicative member of a `to_additive` pair, and at codomain `ℝ` its instance path mismatches; a bare `#check` in the scratch pass passed it. The builder fixed it as § 2 work and proposed row `row_new` 37 of `TRAPS.md`, which is in. The second root was the one sorry, the upper half of `phaseTheta_sub_le`, where `linarith` lacked the product `0 ≤ (T₀ − T) log π`; the block's hint had named neither factor, which `DESIGN.md`'s own checklist line asks for. The foreman closed it with the product, `foreman_edits` 1 edit and `foreman_builds` 1 build. The remaining `cascade` 5 lines were the pins of the sorried chain, row `row_cascade` 19. Fourteen of fifteen theorems closed on the builder's one try, the `T = 2` comparison among them. `assumed_pins` 5 pins, all now in the block: the additive name; `Real.one_lt_pi` does not exist and `div_le_iff` is `div_le_iff₀` here; membership in `riemannZeta.zeroes_rect` has to be opened by `simp only` at every site since it is a definition over a set-builder; the `Finset` of `exists_good_below` packaged through one `obtain`; and the `T = 2` proof getting `N` from `argS` by `unfold; ring` rather than through `stmtArgIdentity_holds`. Module at `jobs_module` 8720 jobs, package at `jobs_package` 8768. Built under the loop at version `loop_version` 19; the retrospective took it to `loop_next` 20: a name the block got wrong is § 2 work and an ASSUMED pin rather than the one try, and a scratch name is an `example` at the module's own types rather than a `#check`.

The measure. The builder's first pass read `tokens_first` 138381 tokens over `calls_first` 26 calls in `minutes_first` 11 minutes; the second ended at `tokens_end` 152653 tokens after `calls_second` 11 more calls in `minutes_second` 2 minutes. Across the three blocks the first-build error count went `errors_h1` 4, `errors_h2` 12, `errors_first` 7, and the sorries `sorries_h1` 2, `sorries_h2` 6, `sorries_first` 1.

What remains. `good 2`, a single fact (no zero of `ζ` with `0 < Re ρ` on `Im s = 2`) that the tree does not hold and upstream does not state; with it `rvM_crude_of_good_two` is unconditional at `(112, 0, 173)`. The Stirling half's `stirling_a` 97 against the budget. And the NOTEPAD's upstream lines: their consumers take a literal `(0.137, 0.443, 6.1)`, so this band ports as a theorem and not as their leaf's discharge.
