**What was done.** Phase 7 retired the hook fleet in `.claude/settings.json` from 4 hook blocks down to 2.

**Removed from settings.json:**
- PreToolUse/Bash matcher block containing `check_direct_run.py` and `check_bash_guard.py` (lab run enforces direct-run; commit gate covers bash-guard)
- PostToolUse block containing `gate.py` (commit gate catches broken refs)

**Deleted from disk (deregistered, measured as ineffective):**
- `utilities/hooks/check_numbers_in_response.py`
- `utilities/hooks/check_agent_brief.py`
- `utilities/hooks/check_read_range.py`

**Surviving hooks (the 2 guards that protect the envelope):**
- `utilities/hooks/check_response_prefix.py` (Stop) -- enforces required response prefix
- `utilities/hooks/check_protected_write.py` (PreToolUse/Edit|Write|MultiEdit) -- protects sealed units and commitment files

**Retired but kept on disk (unwired, still present as reference):**
- `utilities/hooks/check_direct_run.py`
- `utilities/hooks/check_bash_guard.py`

**Other changes:**
- Fixed 2 tree-state test failures in `tests/test_phase2c.py` (made dynamic instead of hardcoded)
- Wrote `tests/test_phase7.py` with tests covering settings validity, hook counts, deleted/retired file assertions, and surviving hook importability
- Added Phase 8 row to `analysis/2026-09-02/lab_design.md` for the quote gate
- Added 3 deleted hook filenames to `utilities/refs_allowlist.txt` so historical citations pass check_refs

**Gate results:**
- All tests passing
- `check_refs.py` exit 0
