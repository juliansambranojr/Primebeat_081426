**New files created:**
- `lab/index.py` — the `lab index` implementation. Loads all units, generates INDEX.md (units by type, artifact counts, sealed/unsealed status) and INDEX-values.tsv (the reverse map: key to citing units). Reuses `lab check`'s matching logic (`NUM`, `matches`, exemption spans) to determine which keys each unit's prose cites.
- `tests/test_phase3.py` — 18 tests covering both files produced, correct counts by type, reverse mapping (multi-unit citation, single-unit citation, uncited keys excluded), idempotency, units with no results, the smoke fixtures and real units, artifact counts, generated headers, the CLI subcommand, and the reverse map format.
- `INDEX.md` — generated output: 7 units (2 sealed, 5 unsealed), grouped by type (6 run, 1 instrument-fix), with 258 total values.tsv keys.
- `INDEX-values.tsv` — generated output: 105 keys in the reverse map.

**Modified files:**
- `lab/cli.py` — registered `lab index` as a subcommand with `--cwd` argument; updated docstring to mention Phase 3; updated `lab new` help text (INDEX.md now exists).
- `lab/__init__.py` — updated docstring to mention Phase 3.
- `lab/new.py` — updated docstring (INDEX.md now exists, not a future phase).
- `analysis/2026-09-02/lab_design.md` — filled in the Phase 3 row of the phase table with what was actually built.
- `tests/test_phase2c.py` — fixed 2 pre-existing failures: `test_the_real_tree_allocates_after_0308` renamed to `test_the_real_tree_allocates_after_0309` (expects 0310), and `test_the_state_line_names_the_next_record` updated to expect "next unit 0310".

**Count-slot strip:** No authored files within editing permission hold running inventory counts that INDEX.md now owns. The "14 modules" in CLAUDE.md is about Lean modules, which is explicitly outside Phase 3's scope. NOTEPAD.md counts are part of each line's record (they describe what happened), not a drifting inventory. No stripping was needed.

**Gate results:**
- `python3 -m pytest -q` — 262 passed, 0 failed (244 existing + 18 new)
- `lab check` — 0000 fails (by design), 0001-0004 pass, 0308 baselined, 0309 passes
- `lab index` — produces both files
- `python3 utilities/check_refs.py` — 0 broken references
