---
id: 0364
date: 2026-09-08
type: formalization
title: "ReZetaCount.lean: Re zeta > 0 on the line Re s = 2, and the zeros of Re zeta on the segment counted by 15 log T + 73"
refs: [lean_stage3/Stage3/ReZetaCount.lean::re_zeta_two_line_pos, lean_stage3/Stage3/ReZetaCount.lean::F_count_le, lean_stage3/Stage3/ReZetaCount.lean::mem_reZeros, lean_stage3/Stage3/ReZetaCount.lean::card_reZeros_le]
supersedes: []
follows: 0363
context: the second of the three blocks of the count-to-argument step; H1 (unit 0363) bounded the contour by a zero count it took as a parameter and a positivity it took as a hypothesis, and this block earns both so that H3 can assemble StmtSCrude argS
sealed: false
---

**Question.** Block `H2` of the hNT worksheet (`hnt.md#h2`). Unit 0363's `zetaArgContour_le` reads two things as hypotheses: `Re ζ(2 + it) > 0` along the vertical leg, and a finite set `Z` holding every zero of `x ↦ Re ζ(x + iT)` on `(1/2, 2)`, with the bound growing in `|Z|`. Does the Dirichlet series give the first, and does Jensen's count on a normalised auxiliary function give the second as a `Finset` with `|Z| ≤ 15 log T + 73`?

**What was proved.** `module_lines` 474 lines, `theorems_proved` 15 theorems, `defs` 4 definitions, `pins` 4 axiom pins each at `axioms` 3 axioms, `sorries` 0 sorries, and `zeta_refs` 36 references to `riemannZeta`.

The line. On `Re s = 2` the series `ζ(s) = Σ (n+1)^{-s}` peels its first term, and the rest is bounded in norm by `Σ (n+2)^{-2} = π²/6 − 1 < 1` (`norm_zeta_sub_one_le`, through `zeta_eq_tsum_one_div_nat_add_one_cpow` and `hasSum_zeta_two`), so `Re ζ(2 + it) ≥ 2 − π²/6 > 0` (`re_zeta_two_line_ge`, `re_zeta_two_line_pos`) at every real `t`, with `π < 3.15` enough.

The segment. `G_T(z) = ζ(2z+2+iT) + ζ(2z+2−iT)` is real on the real axis by `riemannZeta_conj` (`G_real`), equals `2 Re ζ(2 + iT)` at `0` (`G_zero_eq`, `G_zero_ne`), and its real zeros at `z = (x − 2)/2` are the zeros of `Re ζ(x + iT)`. `F_T = G_T / G_T(0)` is `1` at the centre (`F_zero_eq_one`), analytic on the closed unit disk because both arguments stay off the pole at `1` when `‖z‖ < 11/10` at height `T ≥ 2` (`arg_ne_one`, `F_analytic`), and bounded by `disk_bound` 80 times `T` on the `15/16` disk: `zeta_disk` 28 times `T` for each `ζ` value by `Stage3.zeta_disk_upper`, `numer_bound` 56 in all, over a denominator at least `denom_bound` 7/10 (`F_bound`). Upstream's `ZerosBound` at `jensen_r` 7/8 and `jensen_big_r` 15/16 then gives `F_count_le`: the zeros of `F_T` in the `7/8` disk number at most `count_a1` 15 times `log T` plus `count_a3` 73, the arithmetic of `Stage3.zeta_local_zero_count`, whose `local_count_b` 84 is the larger disk bound. `reZeroSet_finite` pulls the segment's zero set back through `x ↦ (x − 2)/2` into that disk, `reZeros` is its `Finset`, `mem_reZeros` its membership, and `card_reZeros_le` its cardinality bound, through `Finset.card_le_card_of_injOn` and the order-at-a-zero step (the order in `ℕ∞` is neither `0`, since `F_T` vanishes there, nor `⊤`, since `F_T(0) = 1` and the identity principle on the unit ball).

**The build, and the relay.** `errors_first` 12 error lines from `roots` 6 root causes at the first build, `sorries_first` 6 sorries at the builder's stop, `warnings_first` 2 warnings. The builder followed the relay's one try per failing step and sorried the step, leaving the surrounding proofs intact. The foreman closed all six on one rebuild, `foreman_edits` 7 edits and `foreman_builds` 4 builds in all: the two `tsum` peels by rewriting the `n = 0` term alone instead of `norm_num` on the hypothesis, the conjugation identity by adding `sub_eq_add_neg` to the `simp` set, the `abs_le` split with both endpoint facts, the `ℕ∞` step by `rcases` on `ENat.toNat_eq_zero`, and the `MapsTo` branch by beta-reducing before reusing the finiteness branch; then the unused `2 ≤ T` binder dropped from `G_zero_ne` and from `F_zero_eq_one`, which the first drop exposed, with their call sites (row `row_binder` 20; the block corrected to match). `traps_added` 4 rows, `row_first` 33 to `row_last` 36: `norm_num` splitting a `tsum` into a different `linarith` atom, `simp [Complex.ext_iff]` leaving a minus against a plus-negative, `rw` against an unreduced application, and `simp only` not unfolding `Ne`. Row `row_ns` 32 recurred at the scratch pass: the block wrote `JensenCount.zeta_disk_upper` for a declaration in `namespace Stage3`, the same trap H1 paid for, caught before the build both times. Row `row_cascade` 19, the four pins failing while their theorems held sorries.

`assumed_pins` 5 pins at the build, all now in the block. One was an arithmetic error in the block: it named `Real.pi_lt_d2`, `pi_crude` 3.15, for the denominator `4 − π²/3 ≥ 7/10`, and that bound gives only `pi_bound_crude` 0.69, so `56T / 0.69` exceeds `80T`. The module uses `Real.pi_lt_d4`, `pi_sharp` 3.1416, which gives `denom_floor` 0.71 and carries the `7/10`. The block's own closing arithmetic line had derived the `0.69` and then divided by `0.7`; the builder read the hint against the closing arithmetic line and reported it, and the block now says so. Module at `jobs_module` 8714 jobs, package at `jobs_package` 8767. Built under the loop at version `loop_version` 18.

The measure. The builder's first pass read `tokens_first` 182812 tokens over `calls_first` 51 calls in `minutes_first` 18 minutes; the second ended at `tokens_end` 199006 tokens after `calls_second` 16 more calls in `minutes_second` 3 minutes. Against H1's `errors_h1` 4 (unit `unit_h1` 0363) the first-build error count went up; the module is larger and the six sorries were six different one-line traps, four of them new rows.

What remains. Block `H3`, written and pinned: at a good height `argS_eq_zetaArgContour` and H1 with `Z = reZeros hT` give `15 log T + 73 + 3/2`; at a bad height above `2` a good height just below with `N` equal and `phaseTheta` moving by at most a half; at `T = 2`, where no good height below is inside the tree's range and the tree holds no first-zero height, a good height just above with `N` monotone and the Stirling phase bound. That yields `StmtSCrude argS 15 600` unconditionally (`sc_b3_two` 600) and `15 75` under "no zero at height 2" (`sc_b3` 75), hence the bands at `band_a` 112 with `band_b3` 698 and `band_b3_good` 173, the `band_a` 112 over entry 130's census budget of `budget` 100 as the worksheet head recorded.
